/**
 * The worker only needs the two lines of copy it burns into the branded
 * end card — the full brand definition (bundle IDs, support email, etc.)
 * lives in `mobile/src/constants/brand.ts` and is the source of truth for
 * wording; keep this in sync by hand if the wordmark or tagline changes.
 */
export const DAYLINE_END_CARD_TEXT = 'Dayline\nFive seconds at a time.';
